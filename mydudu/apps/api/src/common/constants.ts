/**
 * Centralized project parameters and thresholds.
 * Add or adjust these values as needed.
 */

export const NUTRITION_THRESHOLDS = {
    // Current threshold for stunting: Weight < 10kg AND Height < 80cm
    STUNTED: {
        WEIGHT: 10,
        HEIGHT: 80,
    },
    // Current threshold for wasting: Weight < 5kg
    WASTED: {
        WEIGHT: 5,
    },
};
